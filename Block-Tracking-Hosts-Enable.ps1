@echo off
:: https://privacy.sexy â€ v0.13.6 â€ Sun, 20 Oct 2024 23:25:10 GMT
:: Ensure admin privileges
fltmc >nul 2>&1 || (
    echo Administrator privileges are required.
    PowerShell Start -Verb RunAs %0 2> nul || (
        echo Right-click on the script and select Run as administrator.
        pause & exit 1
    )
    exit 0
)
:: Initialize environment
setlocal EnableExtensions DisableDelayedExpansion


:: ----------------------------------------------------------
:: --------------Block Dropbox telemetry hosts---------------
:: ----------------------------------------------------------
echo --- Block Dropbox telemetry hosts
:: Add hosts entries for telemetry.dropbox.com
$domain =telemetry.dropbox.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for telemetry.v.dropbox.com
$domain =telemetry.v.dropbox.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: --------------Block Spotify Live Tile hosts---------------
:: ----------------------------------------------------------
echo --- Block Spotify Live Tile hosts
:: Add hosts entries for spclient.wg.spotify.com
$domain =spclient.wg.spotify.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: -------------Block Windows crash report hosts-------------
:: ----------------------------------------------------------
echo --- Block Windows crash report hosts
:: Add hosts entries for oca.telemetry.microsoft.com
$domain =oca.telemetry.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for oca.microsoft.com
$domain =oca.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for kmwatsonc.events.data.microsoft.com
$domain =kmwatsonc.events.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: -----------Block Windows error reporting hosts------------
:: ----------------------------------------------------------
echo --- Block Windows error reporting hosts
:: Add hosts entries for watson.telemetry.microsoft.com
$domain =watson.telemetry.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for umwatsonc.events.data.microsoft.com
$domain =umwatsonc.events.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for ceuswatcab01.blob.core.windows.net
$domain =ceuswatcab01.blob.core.windows.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for ceuswatcab02.blob.core.windows.net
$domain =ceuswatcab02.blob.core.windows.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for eaus2watcab01.blob.core.windows.net
$domain =eaus2watcab01.blob.core.windows.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for eaus2watcab02.blob.core.windows.net
$domain =eaus2watcab02.blob.core.windows.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for weus2watcab01.blob.core.windows.net
$domain =weus2watcab01.blob.core.windows.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for weus2watcab02.blob.core.windows.net
$domain =weus2watcab02.blob.core.windows.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for co4.telecommand.telemetry.microsoft.com
$domain =co4.telecommand.telemetry.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for cs11.wpc.v0cdn.net
$domain =cs11.wpc.v0cdn.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for cs1137.wpc.gammacdn.net
$domain =cs1137.wpc.gammacdn.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for modern.watson.data.microsoft.com
$domain =modern.watson.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: --------Block telemetry and user experience hosts---------
:: ----------------------------------------------------------
echo --- Block telemetry and user experience hosts
:: Add hosts entries for functional.events.data.microsoft.com
$domain =functional.events.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for browser.events.data.msn.com
$domain =browser.events.data.msn.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for self.events.data.microsoft.com
$domain =self.events.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for v10.events.data.microsoft.com
$domain =v10.events.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for v10c.events.data.microsoft.com
$domain =v10c.events.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for us-v10c.events.data.microsoft.com
$domain =us-v10c.events.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for eu-v10c.events.data.microsoft.com
$domain =eu-v10c.events.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for v10.vortex-win.data.microsoft.com
$domain =v10.vortex-win.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for vortex-win.data.microsoft.com
$domain =vortex-win.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for telecommand.telemetry.microsoft.com
$domain =telecommand.telemetry.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for www.telecommandsvc.microsoft.com
$domain =www.telecommandsvc.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for umwatson.events.data.microsoft.com
$domain =umwatson.events.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for watsonc.events.data.microsoft.com
$domain =watsonc.events.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for eu-watsonc.events.data.microsoft.com
$domain =eu-watsonc.events.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: ----------Block remote configuration sync hosts-----------
:: ----------------------------------------------------------
echo --- Block remote configuration sync hosts
:: Add hosts entries for settings-win.data.microsoft.com
$domain =settings-win.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for settings.data.microsoft.com
$domain =settings.data.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: ------------Block location data sharing hosts-------------
:: ----------------------------------------------------------
echo --- Block location data sharing hosts
:: Add hosts entries for inference.location.live.net
$domain =inference.location.live.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for location-inference-westus.cloudapp.net
$domain =location-inference-westus.cloudapp.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: ------------Block maps data and updates hosts-------------
:: ----------------------------------------------------------
echo --- Block maps data and updates hosts
:: Add hosts entries for maps.windows.com
$domain =maps.windows.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for ecn.dev.virtualearth.net
$domain =ecn.dev.virtualearth.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for ecn-us.dev.virtualearth.net
$domain =ecn-us.dev.virtualearth.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for weathermapdata.blob.core.windows.net
$domain =weathermapdata.blob.core.windows.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: --------Block Spotlight ads and suggestions hosts---------
:: ----------------------------------------------------------
echo --- Block Spotlight ads and suggestions hosts
:: Add hosts entries for arc.msn.com
$domain =arc.msn.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for ris.api.iris.microsoft.com
$domain =ris.api.iris.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for api.msn.com
$domain =api.msn.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for assets.msn.com
$domain =assets.msn.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for c.msn.com
$domain =c.msn.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for g.msn.com
$domain =g.msn.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for ntp.msn.com
$domain =ntp.msn.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for srtb.msn.com
$domain =srtb.msn.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for www.msn.com
$domain =www.msn.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for fd.api.iris.microsoft.com
$domain =fd.api.iris.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for staticview.msn.com
$domain =staticview.msn.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for mucp.api.account.microsoft.com
$domain =mucp.api.account.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for query.prod.cms.rt.microsoft.com
$domain =query.prod.cms.rt.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: ------------Block Cortana and Live Tiles hosts------------
:: ----------------------------------------------------------
echo --- Block Cortana and Live Tiles hosts
:: Add hosts entries for business.bing.com
$domain =business.bing.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for c.bing.com
$domain =c.bing.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for th.bing.com
$domain =th.bing.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for edgeassetservice.azureedge.net
$domain =edgeassetservice.azureedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for c-ring.msedge.net
$domain =c-ring.msedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for fp.msedge.net
$domain =fp.msedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for I-ring.msedge.net
$domain =I-ring.msedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for s-ring.msedge.net
$domain =s-ring.msedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for dual-s-ring.msedge.net
$domain =dual-s-ring.msedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for creativecdn.com
$domain =creativecdn.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for a-ring-fallback.msedge.net
$domain =a-ring-fallback.msedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for fp-afd-nocache-ccp.azureedge.net
$domain =fp-afd-nocache-ccp.azureedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for prod-azurecdn-akamai-iris.azureedge.net
$domain =prod-azurecdn-akamai-iris.azureedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for widgetcdn.azureedge.net
$domain =widgetcdn.azureedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for widgetservice.azurefd.net
$domain =widgetservice.azurefd.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for fp-vs.azureedge.net
$domain =fp-vs.azureedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for ln-ring.msedge.net
$domain =ln-ring.msedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for t-ring.msedge.net
$domain =t-ring.msedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for t-ring-fdv2.msedge.net
$domain =t-ring-fdv2.msedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: Add hosts entries for tse1.mm.bing.net
$domain =tse1.mm.bing.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: -------------Block Edge experimentation hosts-------------
:: ----------------------------------------------------------
echo --- Block Edge experimentation hosts
:: Add hosts entries for config.edge.skype.com
$domain =config.edge.skype.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: ---------------Block Photos app sync hosts----------------
:: ----------------------------------------------------------
echo --- Block Photos app sync hosts
:: Add hosts entries for evoke-windowsservices-tas.msedge.net
$domain =evoke-windowsservices-tas.msedge.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: --------------Block OneNote Live Tile hosts---------------
:: ----------------------------------------------------------
echo --- Block OneNote Live Tile hosts
:: Add hosts entries for cdn.onenote.net
$domain =cdn.onenote.net; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: ----------------------------------------------------------
:: --------------Block Weather Live Tile hosts---------------
:: ----------------------------------------------------------
echo --- Block Weather Live Tile hosts
:: Add hosts entries for tile-service.weather.microsoft.com
$domain =tile-service.weather.microsoft.com; $hostsFilePath = ^$env:WINDIR\System32\drivers\etc\hosts^; $comment = ^managed by privacy.sexy^; $hostsFileEncoding = [Microsoft.PowerShell.Commands.FileSystemCmdletProviderEncoding]::Utf8; $blockingHostsEntries = @(; @{ AddressType = ^IPv4^;  IPAddress = 0.0.0.0; }; @{ AddressType = ^IPv6^;  IPAddress = ::1; }; ); try { $isHostsFilePresent = Test-Path -Path $hostsFilePath -PathType Leaf -ErrorAction Stop; } catch { Write-Error ^Failed to check hosts file existence. Error: $_^; exit 1; }; if (-Not $isHostsFilePresent) { Write-Output ^Creating a new hosts file at $hostsFilePath.^; try { New-Item -Path $hostsFilePath -ItemType File -Force -ErrorAction Stop | Out-Null; Write-Output ^Successfully created the hosts file.^; } catch { Write-Error ^Failed to create the hosts file. Error: $_^; exit 1; }; }; foreach ($blockingEntry in $blockingHostsEntries) { Write-Output ^Processing addition for $($blockingEntry.AddressType) entry.^; try { $hostsFileContents = Get-Content -Path ^$hostsFilePath^ -Raw -Encoding $hostsFileEncoding -ErrorAction Stop; } catch { Write-Error ^Failed to read the hosts file. Error: $_^; continue; }; $hostsEntryLine = ^$($blockingEntry.IPAddress)`t$domain $([char]35) $comment^; if ((-Not [String]::IsNullOrWhiteSpace($hostsFileContents)) -And ($hostsFileContents.Contains($hostsEntryLine))) { Write-Output Skipping, entry already exists.; continue; }; try { Add-Content -Path $hostsFilePath -Value $hostsEntryLine -Encoding $hostsFileEncoding -ErrorAction Stop; Write-Output Successfully added the entry.; } catch { Write-Error ^Failed to add the entry. Error: $_^; continue; }; }
:: ----------------------------------------------------------


:: Pause the script to view the final state
pause
:: Restore previous environment settings
endlocal
:: Exit the script successfully
exit /b 0

